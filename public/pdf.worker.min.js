// Local PDF Processing Worker
// Ensure the actual worker logic is placed here to process without relying on external CDNs
self.onmessage = function (e) {
  console.log('PDF worker received message')
}
