// Local PDF Processing Worker
// This file serves as the local worker context for pdfjs-dist.
// During a standard build process, the actual pdfjs worker script is copied here to ensure no external CDN requests are made.
self.onmessage = function (e) {
  console.log('PDF worker received message', e.data)
}
